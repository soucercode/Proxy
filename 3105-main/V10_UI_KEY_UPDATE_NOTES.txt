ProxyVN SHOP DHP — UI / License update

- Removed the visible grid overlay from ProxyBackground and TechBackground.
- Moved the active key summary to a bottom safe-area inset on the home screen.
- Added ProxyVN SHOP DHP branding with a blue verification-style badge.
- Stored activated key remains in UserDefaults; startup verification keeps it active when valid.
- Added a live 30-second refresh for the remaining-time display.
- Expired status displays “Hết hạn”.
- Server lock now returns the requested admin lock message.
- Server verify now returns a human-readable error for banned/expired/not-found states.
- Added unsupported-iOS gate using the configured support ranges.
- Minimum iOS remains 16.0.
- Text input continues to use the native iOS keyboard; no custom keyboard is bundled.
- Removed the artificial 3-second license activation delay.

Safety boundary:
The existing patch service in this project remains a local/demo patch flow and does not modify
another app's real sandbox. No code was added to patch or alter Free Fire/Free Fire Max files.
